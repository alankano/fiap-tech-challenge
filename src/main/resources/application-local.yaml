spring:
  application:
    name: tech-challenge
  server:
    port: ${SERVER_PORT:8080}
  #h2:
  #  console:
  #    enabled: true
  #    path: /h2-console
  datasource:
    #url: ${DB_URL:jdbc:h2:mem:localtech}
    #username: ${DB_USERNAME:sa}
    #password: ${DB_PASSWORD:password}
    #driver-class-name: ${DB_DRIVER_CLASS_NAME:org.h2.Driver}
    #hibernate:
    #  ddl-auto: update
    #properties:
    # allow Hibernate to auto-detect dialect
    url: ${DB_URL:jdbc:mysql://localhost:3306/my_database}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: ${DB_DRIVER_CLASS_NAME:com.mysql.cj.jdbc.Driver}
  jpa:
    database: mysql
    show-sql: true
  sql:
    init:
      mode: always
      data-locations: classpath:data.sql